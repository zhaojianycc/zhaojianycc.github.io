# Repository instructions

Before making any change in this repository, run the following command from the repository root:

```powershell
git pull --rebase
```

This repository contains a subdirectory maintained by another agent project. Preserve changes made outside the files required for the current task, and resolve any rebase conflict before continuing.

